#!/usr/bin/env sh

if ! gem list foreman -i --silent; then
  echo "Installing foreman..."
  gem install foreman
fi

if [ "$1" = "custom_dev" ]; then
  shift
  exec foreman start -f Procfile.custom_dev "$@"
else
  exec foreman start -f Procfile.dev "$@"
fi
